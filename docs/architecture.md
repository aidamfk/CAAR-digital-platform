System Architecture

Frontend (Website / Mobile App)
        ↓
framework js + Express API (Backend)
        ↓
MySQL Database

The frontend communicates with the backend through REST API.
The backend handles business logic and connects to the database.
